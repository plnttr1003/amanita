extends ../../layout

block title
	title УСЛУГИ

block content_style
	link(rel='stylesheet', type='text/css', href='/build/css/add.css')

block content_scripts
	script(type='text/javascript', src='/build/js/lists.js')

block content
	a.back_link(href='/auth') < Назад
	.title Услуги
	br
	b
		a(href='/auth/serviceship')= ' Редактировать вводную часть'
	br
	br
	a.add(href='/auth/services/add') добавить материал
	br
	br
	.main_menu_block
		each service in services
			.officials
				a.rm_service(href='#', id=service._id)= 'удалить '
				!=' | '
				a(href='/auth/services/edit/' + service._id)= service.i18n.title.get('ru')